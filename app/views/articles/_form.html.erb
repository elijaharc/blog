<% if @article.errors.any? %>
    <h3>The following errors prevented the article from being saved</h3>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
    </ul>
<% end %>

<%= form_with(model: @article, local: true) do |f| %>

<%# new uses:

<%= form_with scope: :article, url: articles_path, local: true do |f| %>

<div id="formPartial">
    <form>
        <div class="form-group">
            <%= f.label :title, "Title", class: "col-md-4 control-label" %>
            <div class="col-md-8">
            <%= f.text_field :title, class: "form-control" %>
            </div>
        </div>
        <div class="form-group">
            <%= f.label :author, "Author", class: "col-md-4 control-label" %>
            <div class="col-md-8">
            <%= f.text_field :author, class: "form-control" %>
            </div>
        </div>
        <div class="form-group">
            <%= f.label :body, "Body", class: "col-md-4 control-label" %>
            <div class="col-md-8">
            <%= f.text_field :body, class: "form-control" %>
            </div>
        </div>
        <p><%= f.submit%></p>
    </form>
</div>
<% end %>